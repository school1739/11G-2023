matrix = [[2, 4, 1, 1], [0, 2, 0, 0], [2, 1, 1, 3], [4, 0, 2, 3]]
x = matrix[0][0]
y =matrix[0][0]
i = (x, y)
def f(matrix, x, y):
    temp = matrix[0].pop(0)
   # for i in range(len(matrix)):

        #matrix.remove(matrix[0][0])
       # matrix.remove(matrix[0][0])


    return matrix

print(f(matrix, 0, 0))



